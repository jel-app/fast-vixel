export declare function createCube(sx: number, sy: number, sz: number, nx: number, ny: number, nz: number): {
    positions: number[][];
    normals: number[][];
    uvs: number[][];
    cells: number[][];
};
